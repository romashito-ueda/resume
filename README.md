# resume (Vite + React) for GitHub Pages

Deploy target:
- https://romashito-ueda.github.io/resume/

## Local dev
```bash
npm install
npm run dev
```

## Deploy
Push to `main`. GitHub Actions builds and deploys to Pages.

## Note
`vite.config.ts` sets `base: "/resume/"` for project pages.
